vectors are seq of data elements of same type
a and bob is vector
how to check if they were vector
is.vector(name of var)
> is.vector(bob)
[1] TRUE
> is.vector(a)
[1] TRUE
both arrays and single values are stored as vector
can create vector by creating
c()
can thik of as combine
int<-c(5L,6L,7L,8L)
to save data type as integers 
add L after each value
to find type
typeof(name)
typeof(int)
[1] "integer"
